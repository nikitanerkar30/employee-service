apiVersion: v1
kind: Service
metadata:
  name: employee-service
  namespace: employee-dev

spec:
  selector:
    app: employee-service
  ports:
    - port: 80
      targetPort: 8080

  type: NodePort      
